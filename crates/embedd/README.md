# embedd

Embedding interfaces + local backends (Candle/HF).

This crate is the embedding substrate used by higher-level systems in the workspace. It aims to
centralize model loading/caching and keep downstream crates backend-agnostic.

## License

MIT OR Apache-2.0
